domain ATQR

context Challenges

ChallengeRepository

  💬showChallenge? :challengeId
    if find challegeId in database == :challengeId
      return challenge
    else
      return "Couldn't find this challenge"
