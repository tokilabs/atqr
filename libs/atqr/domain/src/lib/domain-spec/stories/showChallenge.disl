domain ATQR

context Challenges

ChallengeRepository

  💬showChallenge? :challengeId
    @prismaService.findOne?
      where challegeId == :challengeId
      return challenge
    else
      return "Couldn't find this challenge"
