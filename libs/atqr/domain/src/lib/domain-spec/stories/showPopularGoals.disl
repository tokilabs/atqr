domain ATQR

context Challenges

  showPopularGoals? :qty
    rules:
      - Challenges have to exist in the database

    //call a function that will return a array with arrays of goals that are the same
    groupByGoals!
    show Goals :qty in descending order by lenght of array







