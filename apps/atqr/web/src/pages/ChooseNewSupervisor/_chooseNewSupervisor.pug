doctype html
head
  meta(charset='utf-8')
  meta(http-equiv='x-ua-compatible' content='ie=edge')
  title Escolha um novo supervisor
  meta(name='robots' content='noindex, follow')
  meta(name='description' content='')
  meta(name='viewport' content='width=device-width, initial-scale=1, shrink-to-fit=no')
// Contact Form Start
.contact-form-section.section-padding
  .container
    // Contact Wrap Start
    .contact-wrap
      .row.justify-content-center
        .col-lg-10
          .form-title.text-center
            h2.title Escolha um novo supervisor
          // Contact Form Wrap Start
          .contact-form-wrap
            form(action='#')
              .row
                .col-md-6
                  // Single Form Start
                  .single-form
                    input#name.form-control(type='text' placeholder='Nome do Supervisor')
                  // Single Form End
                .col-md-6
                  // Single Form Start
                  .single-form
                    input#email.form-control(type='email' placeholder='Email do Supervisor')
                    small#invalidEmail
                  // Single Form End
              .col-md-6
              .col-md-12
                // Single Form Start
                .form-btn.text-center
                  button.btn.btn-primary(type='submit' disabled='disabled') Salvar Altera&ccedil;&otilde;es
                // Single Form End
        // Contact Form Wrap End
  // Contact Wrap End
// Contact Form End
// back to top start
.progress-wrap
  svg.progress-circle.svg-content(width='100%' height='100%' viewbox='-1 -1 102 102')
    path(d='M50,1 a49,49 0 0,1 0,98 a49,49 0 0,1 0,-98')
// back to top end
// Javascript
script(src='.\ChooseNewSupervisor.js')
